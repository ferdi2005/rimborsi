<div class="container-fluid">
  <h1><%= t('roles.edit.title') %></h1>
  <%= render 'form', role: @role %>
  <%= link_to t('common.actions.show'), @role, class: "btn btn-success" %>
  <%= link_to t('common.actions.back'), roles_path, class: "btn btn-secondary" %>
</div>
