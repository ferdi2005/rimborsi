<%= bootstrap_form_with(model: state) do |form| %>
<% if state.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>
  <ul>
    <% state.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>
  <div class="form-group mb-3">
          <%= form.label :label, class: "form-label" %>
      <%= form.text_field :label, class:"form-control" %>
      </div>
<div class="form-group mb-3">
  <%= form.submit class: 'btn btn-primary' %>
</div>
<% end %>
