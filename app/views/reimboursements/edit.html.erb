<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-12">
      <div class="card">
        <div class="card-header">
          <h2 class="mb-0">Modifica Rimborso #<%= @reimboursement.id %></h2>
        </div>
        <div class="card-body">
          <%= render 'form', reimboursement: @reimboursement %>
        </div>
        <div class="card-footer">
          <%= link_to 'Visualizza', @reimboursement, class: "btn btn-success" %>
          <%= link_to 'Indietro', reimboursements_path, class: "btn btn-secondary" %>
        </div>
      </div>
    </div>
  </div>
</div>
