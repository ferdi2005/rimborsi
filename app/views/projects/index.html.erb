<div class="container-fluid">
  
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="fas fa-project-diagram me-2"></i>Fondi</h1>
    <% if admin_user? %>
      <%= link_to new_project_path, class: "btn btn-primary" do %>
        <i class="fas fa-plus me-1"></i>Nuovo Progetto
      <% end %>
    <% end %>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card shadow-sm">
        <div class="card-body">
          <div class="table-responsive">
            <table class="table table-hover">
              <thead class="table-light">
                <tr>
                  <th><i class="fas fa-tag me-1"></i>Nome</th>
                  <th><i class="fas fa-euro-sign me-1"></i>Budget</th>
                  <th><i class="fas fa-chart-pie me-1"></i>Spese Totali</th>
                  <th><i class="fas fa-percentage me-1"></i>% Utilizzato</th>
                  <th><i class="fas fa-money-bill-wave me-1"></i>Rimanente</th>
                  <th><i class="fas fa-toggle-on me-1"></i>Attivo</th>
                  <% if admin_user? %>
                    <th>Azioni</th>
                  <% else %>
                    <th>Dettagli</th>
                  <% end %>
                </tr>
              </thead>
              <tbody>
                <% @projects.each do |project| %>
                <tr class="<%= 'table-warning' if project.over_budget? %>">
                  <td>
                    <strong><%= project.name %></strong>
                  </td>
                  <td>
                    <span class="text-primary">€ <%= number_with_precision(project.budget, precision: 2) %></span>
                  </td>
                  <td>
                    <span class="<%= project.over_budget? ? 'text-danger' : 'text-success' %>">
                      € <%= number_with_precision(project.total_expenses, precision: 2) %>
                    </span>
                  </td>
                  <td>
                    <div class="progress" style="height: 20px;">
                      <div class="progress-bar <%= project.over_budget? ? 'bg-danger' : 'bg-success' %>" 
                           role="progressbar" 
                           style="width: <%= [project.budget_percentage_used, 100].min %>%"
                           aria-valuenow="<%= project.budget_percentage_used %>" 
                           aria-valuemin="0" 
                           aria-valuemax="100">
                        <%= project.budget_percentage_used %>%
                      </div>
                    </div>
                  </td>
                  <td>
                    <span class="<%= project.remaining_budget < 0 ? 'text-danger' : 'text-success' %>">
                      € <%= number_with_precision(project.remaining_budget, precision: 2) %>
                    </span>
                  </td>
                  <td>
                    <% if project.active? %>
                      <span class="badge bg-success"><i class="fas fa-check me-1"></i>Attivo</span>
                    <% else %>
                      <span class="badge bg-secondary"><i class="fas fa-times me-1"></i>Inattivo</span>
                    <% end %>
                  </td>
                  <% if admin_user? %>
                    <td>
                      <div class="btn-group" role="group">
                        <%= link_to project, class: "btn btn-outline-primary btn-sm", title: "Visualizza" do %>
                          <i class="fas fa-eye"></i>
                        <% end %>
                        <%= link_to edit_project_path(project), class: "btn btn-outline-secondary btn-sm", title: "Modifica" do %>
                          <i class="fas fa-edit"></i>
                        <% end %>
                        <%= link_to project, method: :delete, 
                                    class: "btn btn-outline-danger btn-sm", 
                                    title: "Elimina",
                                    data: { 
                                      confirm: "Sei sicuro di voler eliminare questo progetto? Tutte le spese associate verranno scollegate dal progetto." 
                                    } do %>
                          <i class="fas fa-trash"></i>
                        <% end %>
                      </div>
                    </td>
                  <% else %>
                    <td>
                      <% if admin_user? %>
                        <%= link_to project, class: "btn btn-outline-primary btn-sm", title: "Visualizza" do %>
                          <i class="fas fa-eye me-1"></i>Dettagli
                        <% end %>
                      <% else %>
                        <span class="text-muted">
                          <i class="fas fa-lock me-1"></i>Solo Admin
                        </span>
                      <% end %>
                    </td>
                  <% end %>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          
          <% if @projects.empty? %>
          <div class="text-center py-5">
            <i class="fas fa-project-diagram fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">Nessun progetto trovato</h5>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
